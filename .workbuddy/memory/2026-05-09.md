# 2026-05-09

## 完成工作

### 1. 两页分离 + React Router
- 安装 `react-router-dom`
- 首页 `/` 只含 Hero
- 详情页 `/details` 含 Timeline × 3 / CaseStudy / HonorsGrid / Footer
- ScrollHandler 组件处理锚点跳转（hash → scrollIntoView）

### 2. Hero 标题格式修复
- 原：混用 `<span class="font-serif italic">法理</span>` 中文英文样式混排
- 改为：纯汉字三行排列，`跨越法理与代码` / `构筑真实世界的` / 大字`合规`（accent色）
- 删除「查看项目实践」按钮
- 「了解经历」按钮改为下拉菜单，点击跳转 `/details#anchor`

### 3. Timeline 排版修复
- 原问题：`!isEven` 时文字右对齐+bullet右对齐，视觉混乱
- 修复：文字/bullet 始终左对齐，仅图片左右交替（`imageRight` prop 控制 lg:order）

### 4. 资源文件夹创建
- `public/assets/images/` — 图片
- `public/assets/documents/` — 简历 PDF
- `public/assets/ppt/` — 演示文稿
- 写了 README.md 说明命名规范和引用方式
- constants.ts 中 imageSrc 改为本地路径，加 fallbackImageSrc 备用
